{
  "name": "tamra",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "dev": "npx rimraf dist/ && npx tsc --watch & npx nodemon ."
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3"
  }
}